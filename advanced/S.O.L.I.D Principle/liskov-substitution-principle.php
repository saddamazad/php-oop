<?php
/**
 * if a function/class/object takes a particular type of object as argument/parameter, we can also pass the sub/child classes of that object/Class as argument/parameter
 * 
 * if an object/class does a particular type of job, the sub/child classes of that object/class should also be able to do the same type of job
 * 
 * Short form is `LSP`
 * 
 */
